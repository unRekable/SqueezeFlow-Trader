# Environment Configuration for Remote InfluxDB Setup
# Copy this to .env and update with your actual values

# Server IP Address (your VPS/Server where InfluxDB runs)
SERVER_IP=your.server.ip.here

# Optional: InfluxDB Authentication (if enabled on server)
INFLUX_USERNAME=admin
INFLUX_PASSWORD=admin123

# Optional: SSH Tunnel Configuration (for secure connection)
SSH_USER=your_ssh_user
SSH_PORT=22